/// type aliases

/// i64 meant to represent a membership_id
pub type MembershipId = i64;

/// i32 meant to represent a membership type
pub type MembershipType = i64;

/// i64 menat to represent an instance
pub type InstanceId = i64;

/// i64 meant to represent a character id
pub type CharacterId = i64;

/// i64 that represents a group id
pub type GroupId = i64;

/// u32 meant to represent a hash that comes from the destiny 2 manifest
pub type ManifestHash = i64;
