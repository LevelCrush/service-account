pub mod destiny;

/// u64 that represents the expected format of our unix timestamps
pub type UnixTimestamp = i64;

/// i32 that represents a record id coming from our own database
pub type RecordId = i64;
